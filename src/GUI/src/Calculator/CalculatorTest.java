package Calculator;

import javafx.application.Application;

public class CalculatorTest
{
  public static void main(String[] args)
  {
    //calling launch static method of Application class that takes Calculator.class as a parameter and passes to the start method.
    Application.launch(Calculator.class);
  }
}
