package interfaces;

//Interface works almost same as abstract class. The interface is used for overriding methods or runtime polymorphism.

public interface Camera
{
  void click(); //Same as abstract, interface's methods have no body and ends with semicolon.

  void record();
}


